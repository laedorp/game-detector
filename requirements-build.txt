-r requirements.txt

# PyInstaller 6.22 supports Python 3.14 and provides current hooks for the
# OpenCV wheel used by this project.
pyinstaller>=6.22,<7

# A release job installs exactly one requirements-runtime-*.txt file after this
# file. ONNX Runtime variants all provide the same module and must never be
# stacked in one build environment.
